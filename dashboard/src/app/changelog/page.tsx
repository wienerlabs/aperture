import { UnderConstruction } from '@/components/shared/UnderConstruction';

export default function ChangelogPage() {
  return (
    <UnderConstruction
      title="Changelog"
      description="Release notes and version history will be published here once the changelog pipeline is wired up."
    />
  );
}
